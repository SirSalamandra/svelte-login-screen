// svelte.config.js
import adapter from '@sveltejs/adapter-static'; // <-- IMPORTANT: Make sure this line is correct
import { vitePreprocess } from '@sveltejs/vite-plugin-svelte';

/** @type {import('@sveltejs/kit').Config} */
const config = {
  preprocess: vitePreprocess(),

  kit: {
    // IMPORTANT: Make sure this section uses the static adapter you imported
    adapter: adapter({
      pages: 'build',
      assets: 'build',
      fallback: 'index.html',
      precompress: false,
      strict: true
    })
  }
};

export default config;